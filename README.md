# getfreecash.github.io
muh blog

```
sudo apt-get update
sudo apt-get install software-properties-common
sudo apt-get install ruby-full
sudo gem install jekyll
sudo gem install bundler
sudo git clone https://github.com/PLACEHOLDER-USERNAME/PLACEHOLDER-USERNAME.github.io
cd PLACEHOLDER-USERNAME.github.io
sudo gem install jekyll bundler
cd ~
sudo jekyll new PLACEHOLDER-USERNAME.github.io
sudo nano PLACEHOLDER-USERNAME.github.io/_config.yml
sudo nano PLACEHOLDER-USERNAME.github.io/about.md
sudo git add --all
sudo git commit -m "Reason for the commit"
sudo git push -u origin master
sudo bundle exec jekyll serve
```
